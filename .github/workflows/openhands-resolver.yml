name: Auto-Fix Tagged Issue with OpenHands (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled in public fork
    runs-on: ubuntu-22.04
    steps:
      - name: Explain disabled resolver
        run: |
          echo "The reusable OpenHands resolver is disabled in this public fork."
          echo "It previously accepted public issue/PR text as an autonomous-agent problem statement."
          echo "Keep resolver workflows private unless runner isolation, token scope, and prompt-injection controls are reviewed."
